<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">My Portfolio</h1>
    <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
    <p>
      <% if logged_in?(:site_admin) %>
      <%= link_to "Create new Portfolio Item", new_portfolio_path, class: "btn btn-warning" %>
      <% end %>
      <%= link_to "Find Out More About Me", about_me_path, class: "btn btn-primary my-2" %>
      <%= link_to "My Blog", blogs_path, class: "btn btn-secondary" %>
    </p>
  </div>
</section>